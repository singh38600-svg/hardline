# Contributing

1. Fork and clone the repository.
2. Run `npm install`.
3. Run `npm run check` before opening a pull request.
4. Add tests for every new policy type, adapter, or bypass fix.

Hardline follows one rule: a policy feature is incomplete until it includes an adversarial simulation.
